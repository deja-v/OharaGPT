# LLM provider keys — only one is required at a time.
# The agent auto-selects: GOOGLE_API_KEY takes priority; falls back to GITHUB_TOKEN.

# Option 1 (preferred): Google Gemini Flash — free tier, 15 req/min, 1M tokens/day
# Get key at: https://aistudio.google.com
GOOGLE_API_KEY=

# Option 2 (fallback): GitHub Models — free with any GitHub account, OpenAI-compatible
# Get token at: https://github.com/settings/tokens
GITHUB_TOKEN=
