# Core LangGraph framework
langgraph==1.1.9
langgraph-checkpoint==4.0.3
langgraph-checkpoint-sqlite==3.0.3
langgraph-prebuilt==1.0.11

# LLM providers — auto-selected by get_llm() based on available env vars
langchain-core==1.3.2
langchain-openai==1.2.1        # GitHub Models (OpenAI-compatible, free fallback)
langchain-google-genai==4.2.2  # Gemini Flash (preferred when GOOGLE_API_KEY is set)

# Env + config
python-dotenv==1.2.2

# Data validation for typed graph state
pydantic==2.13.3
